#include <stdio.h>

int main() {
    int x = 10;
    if (x == 5) {
        printf("x vaut 5\n");
    }
    return 0;
}
//Pas de message d'erreur
//Ici, dans la condition if, un seul '=' a été mis alors qu'il fallait en mettre deux